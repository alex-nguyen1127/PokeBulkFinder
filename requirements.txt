# Web framework
flask>=3.0

# HTTP requests (for eBay API)
requests>=2.31

# Data processing
pandas>=2.1
pyarrow>=14.0        # Parquet file format for S3 snapshots

# Database
psycopg2-binary>=2.9 # PostgreSQL driver (binary = no compiler needed)

# AWS
boto3>=1.34          # AWS SDK — S3, Lambda, etc.

# Background scheduler (local dev only — Lambda replaces this in production)
apscheduler>=3.10

python-dotenv>=1.0 